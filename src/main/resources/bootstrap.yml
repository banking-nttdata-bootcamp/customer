spring:
  application:
    name: customer
  config:
    import: "optional:configserver:http://dk-config-server:7000"
  cloud:
    config:
      enabled: true
      label: master
      uri: http://localhost:7000